# Índice

Este pacote contém duas vistas:

- `agents/<nome>/` — organização interna para manutenção.
- `<nome>/` — pasta pronta para copiar diretamente para um repositório.

Use a pasta que fizer mais sentido para o agente que você quer configurar.
